To use dict from python (uvicorn app.main:app --reload)
to start the app (npm start)

to update firebase: npm build, firebase deploy


order of the list, only consider biggest digit
cost of living
use Mui for forms too
login cros
loading screen
